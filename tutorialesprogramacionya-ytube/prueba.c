#include <stdio.h>
/* imprime la tabla Fahrenheit-Celsius
para fahr = 0, 20, ..., 300 */
int main()
{

#define MEM 30
#if MEM > 100
    printf("MEM es mayor de 100");
#endif
#define VALOR 0
#if VALOR == 0
    c = a * b / (VALOR + 1);
#else
    c = a * b / VALOR;
#endif

    return 0;
}
